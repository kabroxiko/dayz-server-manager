export const environment = {
    production: false,
    apiBaseUrl: '', // Use relative URLs for development
    enableMockMode: false, // Always enable mock mode for development
    mockDelay: 300, // Realistic network delay
    features: {
        enableMap: true,
        enablePlayerTracking: true,
        enableFileEditor: true,
        enableSystemMonitoring: true,
    },
}; 