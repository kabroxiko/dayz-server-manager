export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000/api', // Update this to your DayZ server manager API URL
  mockMode: false // Set to false to connect to real backend
};
